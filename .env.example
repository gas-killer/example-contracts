# Copy to .env and fill in PRIVATE_KEY, then `source .env`.
# .env is gitignored — never commit your real key.
#
#   cp .env.example .env
#   # edit .env: paste your funded Sepolia key
#   source .env

# ── Sepolia deployer (FILL THIS IN) ─────────────────────────────────
export PRIVATE_KEY=0xYOUR_FUNDED_SEPOLIA_PRIVATE_KEY
export SEPOLIA_RPC_URL=https://ethereum-sepolia.publicnode.com

# ── Live Gas Killer AVS on Sepolia ──────────────────────────────────
# A snapshot, not a fixed address. Each redeployment of the testnet stack provisions its own
# BLSSignatureChecker, and a consumer wired to a superseded one passes every off-chain check, is
# handed a signed payload, and then reverts InvalidQuorumApkHash (0xe1310aed) on settlement. Confirm
# the pair against the authoritative source before deploying anything you care about:
#
#   https://gaskiller.xyz/docs/solidity/configuration
#
export AVS_ADDRESS=0xdCec8ce0a03848B55989Bcc711e424Ca31d9eeD9
export SIG_CHECKER_ADDRESS=0x4B0fc77f2A40312f5760a039656FDB683ac1415f

# Reference consumer the forked tests in test/live/ derive the whole wiring from. Unset uses the
# default in test/helpers/LiveDeployment.sol; set it to track a redeployment without editing code.
# export GK_LIVE_INSTANCE=0x...

# ── Hosted aggregator (for script/live/gk-trigger.sh) ───────────────
# Bearer token for the testnet aggregator — ask the Gas Killer team; never commit the real value.
export INGRESS_PASSWORD=ASK_THE_GAS_KILLER_TEAM
export GK_RPC=https://ethereum-sepolia.publicnode.com
export GK_FROM=0xff467a85932cF543Df50255f00A8A829c12a3A11
